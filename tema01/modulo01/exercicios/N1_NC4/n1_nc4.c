#include <stdio.h>

int main() {
   // tipo e nome 
   int idade; // = lê recebe, tipo de atribuicao
   int quantidade = 1;
   float altura = 1.75;
   double peso = 90.3;
   char letra = 'A'; //sempre colocar letras entre aspas simples ou duplas
   char nome[20] = "Bruna";// [20] define a quantidade de caracteres

    idade = 25;
    quantidade = 10;
   
}
/* ---DADOS PRIMITIVOS---
        int -> significa que o dado é do tipo inteiro 1, 2, 3...
        float -> também chamado de ponto flutuante ajuda a usar números decimais, porém armazena valores menores 0.5, 0.2, 1.90...
        double -> armazena variaveis decimais, porém armazena valores maiores 0.5, 0.2, 1.90...
        char -> armazena valores de caracteres e strings
   */